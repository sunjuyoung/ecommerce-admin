server:
  port: 8080


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce_admin
    username: postgres
    password: rnrdj123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        format_sql: true
 #       show_sql: true




logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.example.admin: DEBUG

